import { CompletedValueFormatPipe } from './completed-value-format.pipe';

describe('CompletedValueFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedValueFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
