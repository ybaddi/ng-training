import { CompletedFormatPipe } from './completed-format.pipe';

describe('CompletedFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
