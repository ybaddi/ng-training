import { CompletedBadgeFormatPipe } from './completed-badge-format.pipe';

describe('CompletedBadgeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletedBadgeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
